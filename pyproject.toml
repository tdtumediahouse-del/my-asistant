[project]
name = "yordamchi-bot"
version = "0.1.0"
description = "Yordamchi Bot"
requires-python = ">=3.9"
dependencies = [
    "python-dotenv>=1.0",
]

[tool.vercel]
entrypoint = "api.webhook:handler"
